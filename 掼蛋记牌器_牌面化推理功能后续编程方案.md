# 掼蛋记牌器：牌面化推理功能后续编程方案

> 文档目标：把程序从“AI 面板里解释推理”改造成“牌面直接显示确定事实、三名玩家名下显示极短的不确定结论”。  
> 本方案只说明后续编程方法，不修改现有项目文件。  
> 大小王不再加强概率推理：进贡、抗贡、明牌和实际出牌能够确认时，只记录确定归属。

---

## 一、产品目标

实战中没有时间阅读 AI 推理文字，因此主界面必须遵守以下原则：

1. **确定信息直接显示在具体牌面上。**
2. **不确定信息只显示在上家、对家、下家三名玩家名下。**
3. **每名玩家最多显示 2～6条最重要结论。**
4. **每条结论尽量控制在 2～10个字。**
5. **主界面不显示推理过程、长句、概率表和 AI 助手正文。**
6. **所有结论由本地规则、牌池约束和概率更新完成，不依赖大语言模型。**
7. **结论允许增强、减弱或被推翻，不能为了“看起来越来越聪明”而强制单调上升。**

最终界面不是告诉用户“系统为什么这样想”，而是直接告诉用户：

```text
上家：8×2?  顺↑  三↓
对家：小对少  单多
下家：10×1?  炸险
```

牌面上的确定信息则显示为：

```text
[8 我] [8 我] [8 已出·下] [8 已出·对]
[8 ?]  [8 ?]  [8 ?]      [8 ?]
```

当某张未出牌已经能够被硬约束唯一确定时：

```text
[8 上✓]
```

---

## 二、界面显示规则

## 2.1 牌面只显示确定信息

牌面上的标签不显示概率，不显示“可能”，只显示硬事实。

### 可以显示在牌面上的信息

- 自己的手牌；
- 输入的明牌；
- 进贡已知归属；
- 实际已经出过的牌及出牌者；
- 通过完整牌池约束唯一确定的剩余牌归属；
- 残局联合枚举中所有合法方案都一致的牌。

### 不能显示在牌面上的信息

- “大概率在上家”；
- “可能有一对 8”；
- “顺子倾向较高”；
- “可能没有小对子”；
- 单次过牌形成的软判断；
- 没有达到唯一确定条件的推测。


确定信息继续沿用当前玩家颜色：

- 我：黄色；
- 下家：蓝色；
- 对家：紫色；
- 上家：绿色。

---

## 2.2 不确定信息显示在三名玩家下面

每个玩家名下增加一个极简推理条：

```text
上家
[8×2?] [顺↑] [三↓]
```

不显示长句，不显示“根据某某行为推断”，不在主界面显示百分比。

### 推荐短标签体系

#### 点数归属

| 含义 | 标签 |
|---|---|
| 可能至少一张 8 | `8×1?` |
| 可能至少两张 8 | `8×2?` |
| 可能至少三张 8 | `8×3?` |
| 可能有 8 炸弹 | `8炸?` |
| 某点数明显偏多 | `8多` |
| 某点数明显偏少 | `8少` |

#### 牌型结构

| 含义 | 标签 |
|---|---|
| 顺子结构增强 | `顺↑` |
| 顺子结构减弱 | `顺↓` |
| 对子结构增强 | `对↑` |
| 对子结构减弱 | `对↓` |
| 三张/三带结构增强 | `三↑` |
| 三张/三带结构减弱 | `三↓` |
| 单张较多 | `单多` |
| 小对子较少 | `小对少` |
| 炸弹风险 | `炸险` |
| 牌型偏整 | `牌整` |
| 牌型偏散 | `牌散` |

#### 行为提示

| 含义 | 标签 |
|---|---|
| 多次无法接对子 | `对受限` |
| 多次无法接三张 | `三受限` |
| 多次无法接单张 | `单受限` |
| 可能主动保留强牌 | `藏强` |
| 对家牌权下的配合过牌 | 不显示 |
| 旧判断被后续出牌推翻 | 标签自动删除，不显示解释 |

### 置信度不用数字表示

建议通过视觉强度区分：

- 弱线索：灰色描边；
- 较可能：浅色底；
- 强线索：实色底；
- 新变化：短暂闪动一次；
- 被反证：直接消失，不保留红色长提示。

这样用户不需要读取 `63%`、`78%` 等数字，只需看标签是否出现以及颜色深浅。

---

## 2.3 每名玩家最多显示六条

推理结果再多，主界面也只能保留最重要的六条。

优先级建议：

1. 残局硬锁定附近的点数信息；
2. 可能影响当前出牌的牌型限制；
3. 炸弹或大牌风险；
4. 当前轮刚刚显著变化的结构倾向；
5. 其他长期倾向。

同类信息只保留一条。例如：

```text
错误：
[8×1?] [8×2?] [8多]

正确：
[8×2?]
```

如果当前轮在打对子，应提高对子相关标签优先级；当前轮在打三带二，应提高三张和对子结构标签优先级。

---

## 三、核心技术路线：不用 AI，也能像人一样推理

掼蛋属于不完全信息游戏。程序需要维护的不是一段语言解释，而是一个不断更新的“可能手牌集合”。

推荐采用四层推理：

```text
硬事实层
    ↓
牌池与剩余张数约束层
    ↓
行为证据与结构竞争层
    ↓
界面压缩显示层
```

---

## 四、第一层：硬事实推理

硬事实直接改变牌池，不带概率。

## 4.1 已知牌

包括：

- 自己的 27 张手牌；
- 明牌；
- 进贡牌；
- 已经出过的牌；
- 玩家剩余牌数；
- 某玩家已经走完；
- 用户手工确认的信息。

这些信息必须从未知牌池中扣除，不能再次作为软证据重复计算。

---

## 4.2 点数耗尽

每个普通点数共有 8 张。

```text
某点数总数 = 8
未出数量 = 8 - 已出数量
```

如果未出数量为 0：

```text
该点数不可能再出现在任何玩家手中
```

如果未出数量等于某一玩家已经确定持有的数量：

```text
该点数其余玩家全部排除
```

---

## 4.3 唯一归属

只有满足完整约束时，才能把牌直接标在某人名下。

例如：

```text
还剩 2 张 8
上家至少持有 2 张 8
```

则两张都可以确定标记为：

```text
上✓
```

“上家至少持有 2 张”必须来自以下任一硬条件：

- 明牌；
- 进贡；
- 用户确认；
- 残局联合枚举中所有合法方案都包含至少 2 张；
- 其他玩家的剩余槽位和点数上限已经把分配唯一锁死。

不能因为“上家一直没出 8”就直接标记为确定。

---

## 五、第二层：牌池约束与归属概率

## 5.1 用户描述的典型场景

场景：

- 自己手里有某点数；
- 另外两家已经出过该点数；
- 第三家尚未展示；
- 场外仍有该点数未出现。

这会提高第三家持有剩余牌的概率，但通常不是硬事实。

程序应计算：

```text
该点数还剩多少张
每家还剩多少手牌槽位
每家已知持有多少张
每家已经出过多少张
每家对该点数至少有 1、2、3、4 张的概率
```

### 示例

```text
我手中有 2 张 8
下家已出 1 张 8
对家已出 2 张 8
全桌共 8 张 8
还剩 3 张未出现
上家从未展示 8
```

此时不是直接写“3 张都在上家”，而是提高：

```text
上家至少有 1 张 8
上家至少有 2 张 8
```

的后验概率。

界面可能显示：

```text
上家：[8×2?]
```

只有当后续约束把这两张锁死时，才把具体牌面标成：

```text
[8 上✓] [8 上✓]
```

---

## 5.2 从边际概率升级为联合分配

现有项目主要按“某家对某点数的概率”分别计算。后续需要增加单点数联合分配。

对于还剩 4 张 8，应枚举：

```text
下0 对0 上4
下0 对1 上3
下0 对2 上2
下1 对0 上3
下1 对1 上2
……
```

每个分配必须满足：

```text
下家数量 + 对家数量 + 上家数量 + 我的未知数量
= 该点数未出总数
```

并且不能超过每家的剩余手牌数。

这样可以避免：

```text
上家预计 3 张
下家预计 2 张
对家预计 2 张
但实际只剩 4 张
```

这种彼此冲突的显示。

---

## 5.3 完整张数分布

每个玩家、每个点数保存完整分布：

```ts
interface RankCountDistribution {
  playerPosition: PlayerPosition;
  rank: Rank;

  probabilityByCount: number[];
  // 下标表示张数：
  // [P(0张), P(1张), P(2张), P(3张), P(4张)...]

  expectedCount: number;
  minCount: number;
  maxCount: number;
  mostLikelyCount: number;
}
```

界面不直接显示这组数字，但用它决定短标签：

```text
P(至少1张) ≥ 0.70  → 8×1?
P(至少2张) ≥ 0.70  → 8×2?
P(至少4张) ≥ 0.45  → 8炸?
```

阈值后续必须通过测试牌局校准，不能一次写死。

---

## 六、第三层：像人一样分析“为什么这样出牌”

核心不是看到一手牌就套固定结论，而是比较：

> 假设他拥有不同的手牌结构，做出当前选择的合理程度分别有多高。

这就是行为似然更新。

```text
后验权重
=
原有权重
× 当前行为在该手牌假设下出现的可能性
```

主界面不显示公式和解释，只显示压缩后的结论。

---

## 七、限制选择推理

纸牌推理中的“限制选择”可以直接用于本项目：

> 如果一个玩家在多个近似等价的选择中选择了其中一个，那么“他其实没有其他选择”的手牌状态，应获得更高权重。

但不能简单写成：

```text
出了大对子 = 一定没有小对子
```

正确做法是枚举他当时可能的合法响应。

---

## 7.1 小对子被大对子直接压住

场景：

```text
我出 44
对手直接出 QQ
```

程序先假设对手有不同手牌：

### 假设 A：只有 QQ 能接

```text
出 QQ 很自然
行为似然高
```

### 假设 B：同时有 66、88、QQ

```text
正常节省牌力时更可能出 66 或 88
直接出 QQ 的行为似然较低
```

因此观察到 `QQ` 后：

```text
对手持有 66、77、88、99、10、J 等中小对子的概率下降
```

界面显示：

```text
小对少
```

但该结论需要根据场景修正。

### 会削弱“小对少”结论的情况

- 对手只剩很少牌，需要抢牌权；
- 我方有人即将走完，对手必须强压；
- QQ 出完后能顺利接下一手；
- 较小对子属于连对结构，不愿拆；
- 较小对子要作为三带二的对子附件；
- 当前出牌者是他的对家，可能存在配合逻辑；
- QQ 本身是最容易处理的孤立对子。

因此代码不能只看“44 → QQ”，必须计算替代选择成本。

---

## 7.2 替代选择成本

对当时每个合法响应计算成本：

```ts
interface ResponseCost {
  cardsUsed: number;
  rankCost: number;
  breaksPairStraight: boolean;
  breaksStraight: boolean;
  breaksTriple: boolean;
  breaksBomb: boolean;
  usesWildCard: boolean;
  keepsLeadValue: number;
  endgameValue: number;
}
```

如果玩家选择的牌明显比最低成本方案昂贵，则产生结构证据：

```text
有更小对子却不用的假设权重下降
```

如果根本不存在更小的合法对子，则：

```text
小对少
```

证据增强。

项目已经有 `beatSolver.ts`，可以在此基础上新增：

```text
findAllReasonableBeats()
scoreResponseCost()
calculateActionLikelihood()
```

---

## 八、牌型结构竞争推理

同一批牌不能同时无限组成顺子、对子、三张和三带二。牌型之间存在资源竞争。

例如：

- 顺子需要五个连续点数；
- 三带二需要三张同点数加一对；
- 连对需要连续三组对子；
- 钢板需要连续两组三张；
- 炸弹需要同点数至少四张。

因此可以维护每名玩家的结构向量：

```ts
interface PlayerStructureBelief {
  pairScore: number;
  tripleScore: number;
  straightScore: number;
  pairStraightScore: number;
  tripleStraightScore: number;
  bombScore: number;
  looseSingleScore: number;
  confidence: number;
}
```

界面只把明显变化压缩成：

```text
顺↑
三↓
对↓
单多
牌整
牌散
```

---

## 8.1 顺子多，三带少

这不是绝对规则，而是资源竞争倾向。

如果某玩家多次表现出：

- 使用连续点数组成顺子；
- 避免拆开连续中张；
- 对单点高张反应弱；
- 对对子和三张牌型长期受限；
- 出牌后仍可能保留多条连续路线；

则提高：

```text
straightScore
```

同时适度降低：

```text
tripleScore
```

界面显示：

```text
顺↑  三↓
```

但如果该玩家已经把一条顺子打出，程序必须区分：

```text
他此前拥有顺子结构
```

和：

```text
他当前剩余牌仍然有顺子结构
```

已经打掉一条顺子，不应永久让 `顺↑` 越来越高。每次出牌后都要重新计算剩余牌可能结构。

---

## 8.2 三带多，顺子少

如果某玩家多次打出三张或三带二，尤其是所用点数位于自然顺子中段，例如 6、7、8、9、10，则这些点数被集中在同一点数上的可能性增强，连续点数资源相对减少。

可形成：

```text
三↑  顺↓
```

但是三带二中的对子附件不能被错误归入“三张结构”。需要分别记录：

```text
主三张点数
附件对子点数
```

---

## 8.3 出怪三带二暴露结构

例如某玩家主动打：

```text
999 + 33
```

可产生以下软证据：

- 9 至少有三张是硬事实；
- 3 至少有一对是硬事实；
- 9 附近的顺子路线减少；
- 3 可能是孤立小对，也可能是三带附件；
- 如果当时存在其他更自然附件而选择 33，可能说明 33 最难处理；
- 如果在开局主动出怪三带，可能说明其手牌需要快速整理。

界面不显示这些解释，只根据综合结果显示：

```text
三↑
小对少
牌散
```

最多保留其中最重要的两条。

---

## 九、过牌推理

过牌只能作为软证据，且必须区分牌权关系。

## 9.1 对手牌权下过牌

如果面对对手的牌过牌：

```text
可能缺少低成本直接管牌
```

但仍可能：

- 有炸弹而选择保留；
- 有更大牌但不愿拆结构；
- 为后续牌权做准备；
- 判断没有必要争抢。

因此显示只能是：

```text
对受限
三受限
单受限
```

不能显示：

```text
无对子
无三张
无大牌
```

---

## 9.2 对家牌权下过牌

面对对家出牌时的过牌通常属于正常配合，不应形成“缺牌”标签。

该逻辑当前项目已经具备，应继续保留：

```text
对家牌权下过牌 → 默认不进入负面持牌证据
```

---

## 9.3 后续反证

如果某玩家此前对某牌型过牌，后来又打出当时能够管上的组合：

```text
旧缺牌推断自动降级或删除
```

主界面不需要显示“推断被推翻”，只需让旧标签消失。

回放和调试模式可以保留完整变化记录。

---

## 十、残局推理

残局是最适合精确计算的阶段。

现有项目在玩家剩余不超过 5 张时，会逐玩家枚举可能点数组合。后续应升级为三家联合枚举。

## 10.1 联合枚举必须满足

- 所有未出牌不能重复；
- 每家牌数等于其剩余张数；
- 已知手牌和明牌归属固定；
- 所有实际出牌历史成立；
- 用户确认的信息成立；
- 软证据只调整权重，不直接删除合法方案。

## 10.2 硬锁定

如果所有合法联合方案中，上家都至少有两张 10：

```text
牌面直接标记两张：上✓
```

如果只有 78% 的联合方案中上家至少有两张 10：

```text
上家名下显示：10×2?
```

这正好对应“确定信息上牌面、不确定信息在玩家下面”的产品要求。

---

## 十一、大小王处理

大小王不增加复杂概率推理。

只处理以下硬事实：

- 进贡明确知道大王归属；
- 抗贡规则产生的确定信息；
- 用户手牌中的王；
- 明牌中的王；
- 实际出过的王；
- 残局约束唯一锁定的王。

不在玩家名下显示：

```text
大王?
小王?
```

除非残局已经接近唯一确定，并且该信息会直接影响当前决策。默认仍不显示，以减少界面负担。

---

## 十二、推理结果的数据结构

建议在 `src/types/game.ts` 增加：

```ts
export interface PlayerInferenceChip {
  id: string;
  playerPosition: PlayerPosition;

  category:
    | 'rank_count'
    | 'pair_structure'
    | 'triple_structure'
    | 'straight_structure'
    | 'bomb_risk'
    | 'single_structure'
    | 'response_limit';

  label: string;
  confidence: number;
  priority: number;

  sourceActionIds: string[];
  expiresWhenRoundChanges?: boolean;
}

export interface KnownCardOwnership {
  cardId: string;
  owner: PlayerPosition;

  source:
    | 'self_hand'
    | 'revealed'
    | 'tribute'
    | 'played'
    | 'constraint_locked'
    | 'user_confirmed';
}

export interface PlayerStructureBelief {
  playerPosition: PlayerPosition;

  pairScore: number;
  tripleScore: number;
  straightScore: number;
  pairStraightScore: number;
  tripleStraightScore: number;
  bombScore: number;
  looseSingleScore: number;

  confidence: number;
}

export interface TableInferenceViewModel {
  knownOwnership: Record<string, KnownCardOwnership>;
  playerChips: Record<PlayerPosition, PlayerInferenceChip[]>;
}
```

`TableInferenceViewModel` 是主界面唯一需要读取的推理结果。

主界面不直接读取：

- 证据账本；
- 贝叶斯计算过程；
- 完整概率分布；
- 长文本推理；
- AI 建议正文。

---

## 十三、推荐新增模块

```text
src/utils/knownOwnershipResolver.ts
```

职责：

- 汇总自己手牌、明牌、进贡、已出牌；
- 找出约束唯一确定的牌；
- 输出牌面硬标记。

```text
src/utils/jointRankInference.ts
```

职责：

- 对每个点数进行联合张数分配；
- 保证各家分配总数不冲突；
- 输出每家持有 0、1、2、3、4 张的概率。

```text
src/utils/actionLikelihood.ts
```

职责：

- 枚举玩家当时可能的合理响应；
- 比较实际选择与低成本选择；
- 生成“小对少”“藏强”等软证据。

```text
src/utils/structureCompetition.ts
```

职责：

- 计算对子、三张、顺子、连对、钢板、炸弹之间的资源竞争；
- 区分“此前展示过某结构”和“当前剩余牌仍可能保留某结构”。

```text
src/utils/inferenceViewModel.ts
```

职责：

- 将复杂推理压缩为每名玩家最多三条短标签；
- 按当前牌型、残局程度、置信度和新鲜度排序。

```text
src/components/KnownOwnerOverlay.tsx
```

职责：

- 显示牌面硬归属；
- `pointer-events: none`；
- 不影响现有滑动选牌。

```text
src/components/PlayerInferenceStrip.tsx
```

职责：

- 显示玩家名下的 2～3 个短标签；
- 不显示长文字；
- 不占用牌面区域。

```text
src/hooks/useInferenceTimeline.ts
```

职责：

- 保存每次出牌后的推理快照；
- 支持撤销后重新计算；
- 供回放和测试使用；
- 主界面不显示完整时间线。

---

## 十四、现有文件的调整方向

## 14.1 `RankInferenceBadge.tsx`

当前组件在整行右上角显示概率归属和炸弹风险。

建议后续改为：

- 不再显示不确定归属；
- 不再显示百分比；
- 确定信息转移到每张牌的 `KnownOwnerOverlay`；
- 行级组件只在必要时保留极简耗尽标识，例如 `已尽`。

---

## 14.2 `AIAssistant.tsx`

主牌局界面不再承担推理展示。

建议：

- 默认不渲染或保持完全收起；
- 仅在调试模式、回放模式和开发测试中查看完整证据；
- 不把 AI 面板作为实战信息入口。

---

## 14.3 `App-Simple.tsx`

负责把：

```text
analysisResult
```

转换成：

```text
TableInferenceViewModel
```

然后：

- 在每张牌外层渲染 `KnownOwnerOverlay`；
- 在上家、对家、下家区域渲染 `PlayerInferenceStrip`；
- 不在牌面中输出长文本；
- 不在主界面输出概率解释。

---

## 14.4 `humanReasoning.ts`

应增加：

- 自己手牌参与“三方已知、剩余一家”的显式归属推理；
- 替代选择分析；
- 牌型资源竞争；
- 当前剩余结构重算；
- 玩家行为风格与单局牌型证据分离。

---

## 14.5 `cardDistributionInference.ts`

应增加：

- 每家对每个点数的完整张数分布；
- 单点数联合分配；
- 分配总数一致性检查；
- 输出 `mostLikelyCount`；
- 保留当前超几何先验和证据融合方法。

普通点数继续计算，大小王默认不进入软推理展示。

---

## 十五、推理更新流程

每次新增出牌或过牌后执行：

```text
1. 重建硬事实
2. 从未知牌池扣除已知牌
3. 更新每家剩余槽位
4. 对每个点数计算联合张数分配
5. 枚举本次行为的合理替代选择
6. 计算实际选择的行为似然
7. 更新对子、三张、顺子等结构分数
8. 若进入残局，运行三家联合枚举
9. 找出能够硬锁定的具体牌
10. 生成每家最多三条短标签
11. 渲染牌面和玩家推理条
```

撤销操作时：

```text
根据撤销后的完整历史重新计算
```

不要尝试用“概率倒退”恢复旧状态，否则容易残留错误证据。

---

## 十六、行为似然的建议实现

可以使用规则评分，不需要神经网络。

```ts
function calculateActionLikelihood(
  hypotheticalHand: Card[],
  observedAction: PlayRecord,
  context: ActionContext
): number {
  const legalActions = enumerateLegalResponses(
    hypotheticalHand,
    context.currentLead
  );

  const scored = legalActions.map(action => ({
    action,
    cost: scoreActionCost(action, hypotheticalHand, context)
  }));

  const observed = scored.find(item =>
    isSameAction(item.action, observedAction)
  );

  if (!observed) return 0;

  return softmaxLikelihood(observed.cost, scored);
}
```

成本可考虑：

```text
牌点成本
是否拆对子
是否拆三张
是否拆顺子
是否拆连对
是否拆钢板
是否拆炸弹
是否使用逢人配
是否有必要争牌权
对手剩余牌数
出牌者与当前牌权玩家的队伍关系
出牌后剩余手数
```

这样才能把：

```text
我出小对，对手直接出大对
```

转化为合理的软推理，而不是死规则。

---

## 十七、标签生成示例

## 示例一：自己有牌，另两家已展示

已知：

```text
我有两张 8
下家出过一张 8
对家出过两张 8
还剩三张 8 未出现
```

联合推理后：

```text
上家至少两张 8 的概率较高
```

显示：

```text
上家：[8×2?]
```

不在具体 8 的牌面上标 `上`。

后续残局约束确定两张 8 必在上家：

```text
两张具体牌显示：[上✓]
玩家名下删除：[8×2?]
```

---

## 示例二：小对子被大对子直接压住

历史：

```text
我出 44
上家直接出 QQ
```

若系统枚举发现：

- 持有中小对子时通常有更低成本选择；
- 当前又没有必须抢牌权的理由；
- QQ 不是为了保护连对结构而被迫打出；

则显示：

```text
上家：[小对少]
```

若上家只剩 3 张牌，抢牌权非常重要：

```text
不显示“小对少”
```

或者降低到弱灰色，不占前三条。

---

## 示例三：顺子与三带竞争

历史中某玩家：

- 多次保留连续中张；
- 出过顺子；
- 对三张牌型多次受限；
- 当前剩余牌仍能形成多条连续路线。

显示：

```text
下家：[顺↑] [三↓]
```

如果刚刚把唯一顺子打完，剩余路线大幅减少：

```text
删除“顺↑”
```

不能因为历史上出过顺子，就一直认为当前仍有顺子。

---

## 示例四：对家牌权下过牌

对家出对子，上家过牌。

由于二者是同队：

```text
不生成“对受限”
```

避免把正常配合误判为缺牌。

---

## 十八、信息压缩规则

生成标签时执行：

```ts
const MAX_CHIPS_PER_PLAYER = 3;
const MAX_LABEL_LENGTH = 5;
```

### 合并规则

```text
8×1? + 8×2? → 8×2?
顺↑ + 顺多 → 顺↑
三受限 + 三↓ → 保留优先级更高的一条
```

### 过期规则

以下标签只在短期有效：

- 当前牌型受限；
- 本轮刚发生的限制选择；
- 当前待压牌面的反应。

以下标签可以跨轮保留，但必须持续重算：

- 某点数可能有两张；
- 顺子结构；
- 三张结构；
- 炸弹风险。

---

## 十九、测试方法

不能只看“感觉是否聪明”，必须用回放牌局测试。

## 19.1 硬事实测试

确保：

- 已出牌不会重新分配；
- 已知手牌不会分给别人；
- 每个点数分配总数等于实际剩余数；
- 每家推定牌数不超过剩余手牌数；
- 硬锁定牌在所有合法方案中归属一致。

## 19.2 反证测试

构造牌局：

```text
某玩家先对对子过牌
后续打出当时可管的对子
```

预期：

```text
旧“对受限”标签消失
```

## 19.3 队友配合测试

构造：

```text
对家出牌，玩家过牌
```

预期：

```text
不产生缺牌推断
```

## 19.4 限制选择测试

构造两组真实手牌：

```text
A：只有 QQ 能管
B：66、88、QQ 都能管
```

观察到玩家出 QQ 后，A 的后验权重应高于 B。

## 19.5 校准指标

现有项目已有推理校准脚本，后续继续比较：

- Brier Score；
- Log Loss；
- 命中率；
- 错误强提示率；
- 标签平均数量；
- 标签在屏幕上的停留时间；
- 用户是否能在一秒内理解。

最重要的产品指标不是“显示多少推理”，而是：

```text
强提示错误率必须低
```

宁可少显示，也不要经常把错误推断放到牌桌主界面。

---

## 二十、实施顺序

## 第一阶段：只改展示方式

目标：

- 确定信息显示到每张牌；
- 不确定信息显示到三名玩家下面；
- 每人最多三条；
- 不显示 AI 长文；
- 大小王只显示硬事实。

复用现有推理结果，不先重写算法。

---

## 第二阶段：补充人类规则推理

增加：

- 自己手牌参与点数归属推理；
- 小对被大对直接压住的限制选择；
- 替代响应成本；
- 顺子与三带的结构竞争；
- 配合过牌过滤；
- 后续反证删除标签。

---

## 第三阶段：单点数联合分配

增加：

- 每个点数的三家联合张数分配；
- 完整张数概率；
- `8×1?`、`8×2?` 等稳定标签；
- 数量一致性检查。

---

## 第四阶段：残局三家联合枚举

增加：

- 三家手牌同时枚举；
- 所有合法方案共同拥有的牌标为确定；
- 其余高概率结论放在玩家名下；
- 不再出现三家最高概率组合互相争用同一张牌。

---

## 第五阶段：完整可能牌局样本池

当单点数联合分配仍不足时，再增加加权样本池或粒子过滤：

```text
生成若干完整合法手牌分配
每次行为后删除矛盾样本
按行为合理性调整权重
从样本统计归属和结构
```

这一阶段适合进一步提高“像人一样思考”的程度，但不应作为第一阶段的前置条件。

---

## 二十一、最终原则

后续开发始终遵守：

```text
硬事实上牌面
软推断放人名下
一人最多三条
主界面不讲道理
调试页才看证据
不用 LLM 做实时推理
错误强提示比少提示更危险
```

真正适合实战的界面不是信息最多，而是用户抬眼就能看到：

```text
谁确定有牌
谁可能有几张
谁偏顺子
谁缺小对
谁有炸弹风险
```
做得对、而且值得肯定的几处判断
"确定信息上牌面、不确定信息挂人名下"这个二分法本身是对的，而且用两个完全独立的类型 KnownCardOwnership 和 PlayerInferenceChip 来承载，而不是一个带 confidence 字段的统一类型——这个类型设计选得好，从结构上就杜绝了"软推断被误标成确定"这种 bug，比靠代码规范约束可靠得多。
4.3 节"不能因为'上家一直没出8'就直接标记为确定"，这句是全文最重要的一条正确性原则，写得很清楚，必须严格执行。
5.2 节指出边际概率独立计算会导致"三家预计张数加起来超过实际剩余数"的冲突——这是概率推断里一个真实存在、容易被忽略的错误模式（每家单独算，互不校验总和），方案里要求联合分配保证"总数不冲突"，这个修正是必要的。
8 节区分"曾经展示过某结构"和"当前剩余牌是否仍有该结构"——这正好是我上一轮说过的"衰减/时效性"问题，方案里明确要求每次出牌后重新计算剩余结构，而不是让分数只涨不跌，抓得很准。
19.5 节那句"强提示错误率必须低，宁可少显示"——这是整个方案里最重要的产品原则，必须当成硬约束贯穿始终，我完全同意。
动手前必须先解决的几处，不然会跟自己的原则打架

1）Phase 1 说"复用现有推理结果，不先重写算法"，但现有推理结果里，至少有一处我们上一轮刚发现的问题，如果直接复用会违反这份文档自己 19.5 节的原则。

aiReasoningEngine.ts 里 threatLevels 的炸弹风险判断，现在用的是"打出过炸弹类型 = 可能还有炸弹"这个逻辑，方向可能是反的（打出炸弹往往说明已经用掉了）。如果 Phase 1 直接把这类字段映射成牌面上的"炸险"标签，就是在往牌面上放一个可能本身就错的强提示——这恰好撞上文档自己反复强调的"错误强提示比少提示更危险"。**建议在 Phase 1 开工前，先列一张清单：现有 AIAnalysisResult 里哪些字段的可信度已经验证过（比如 cardDistribution.bombCandidates 的超几何后验、humanReasoning.ownershipClues 里 confidence: 'known' 的部分——这个我查过，确实只在有真实已知牌支持时才会置 known，是干净的），哪些还带着已知问题（比如 threatLevels 的炸弹风险来源）。带问题的字段要么先在这一轮里顺手修掉，要么明确排除在 Phase 1 的展示范围外，不能因为"这轮不动算法"就把有问题的字段也原样展示出来。

2）Phase 3"单点数联合分配"，建议先确认是不是在重复造轮子。

cardDistributionInference.ts 里 getAnyOpponentBombProbability 已经在用递归分配枚举做多方联合概率计算了（虽然目前只是为了算"任一对手是否有炸弹"这一个聚合问题）。方案里提议新建 jointRankInference.ts 单独实现"每家持有 0/1/2/3/4 张的完整分布"——这两者背后需要的其实是同一套组合分配逻辑，建议先看能不能把现有那段递归枚举重构成一个通用函数，输出完整的 probabilityByCount 数组，getAnyOpponentBombProbability 和新的联合分配需求共用同一套计算，而不是维护两套相似但独立的枚举代码。上一轮我们才发现 estimatePlayerHands 和 cardDistribution 两套"炸弹风险"逻辑并存导致的问题，这里如果不注意，很容易又出现同一类"重复实现、后续互相不同步"的隐患。

3）显示阈值本身也该纳入校准范围，不只是概率数字。

5.3 节已经提醒"阈值必须通过测试牌局校准"，但这个要求只写在这一节，18 节的 MAX_CHIPS_PER_PLAYER = 3 和 2.2 节"弱线索/较可能/强线索"的视觉分级边界，本质上也是同一类需要校准的magic number，建议统一放进 19.5 节那套 Brier/ECE 校准流程里一起测，不要只校准概率本身、却让"多少概率才显示"这道最终决定用户看到什么的门槛继续靠感觉定。

4）建议补一条方案里没提到的东西：阈值附近的"闪烁"问题。

如果某个点数的 P(至少2张) 在 0.69 和 0.71 之间来回跳（每手牌都会小幅波动），按现在的方案，8×2? 标签会一会儿出现一会儿消失，这在实战中比"一直显示"或"一直不显示"都更干扰。建议给每个标签加滞回区间（hysteresis）：比如达到 0.70 才显示，但要跌到 0.55 以下才隐藏，出现和消失用不同阈值，避免临界值附近来回闪。这是标准的 UI 工程手法，方案目前完全没提，建议补进 18 节。

引用文献部分

第 5 条参考文献（Guandan Theory of Mind, arXiv 2408.02559）我上一轮已经查证过，内容属实。其余几篇（约束满足信念状态、History Filtering、粒子过滤、LAMDA 反绎学习、DanZero+）都是这个领域真实存在的相关研究方向，和文档里引用的场景（隐藏状态建模、MCMC 生成一致历史、粒子过滤维护可能状态集合）匹配，不是编造的，可以放心作为设计参考。
---

## 参考方法与资料

1. Morenville, A. & Piette, É. *Modeling Uncertainty: Constraint-Based Belief States in Imperfect-Information Games*. 讨论用约束满足问题维护隐藏状态，以及用概率传播估计边际概率。  
   https://arxiv.org/abs/2507.19263

2. Solinas, C. et al. *History Filtering in Imperfect Information Games: Algorithms and Complexity*. 讨论从公共信息和玩家信念生成与历史一致的隐藏状态，并给出适合纸牌游戏的 MCMC 生成方法。  
   https://proceedings.neurips.cc/paper_files/paper/2023/hash/87ee1bbac4635e7c948f3eea83c1f262-Abstract-Conference.html

3. Clark, G. *Deep Synoptic Monte-Carlo Planning in Reconnaissance Blind Chess*. 使用粒子过滤维护不完全信息下的可能状态集合。  
   https://proceedings.neurips.cc/paper/2021/hash/215a71a12769b056c3c32e7299f1c5ed-Abstract.html

4. The Bridge World, *Restricted Choice*. 当玩家本可从多个等价选择中选择却只展示其中一个时，“他没有其他选择”的状态应获得更高权重。  
   https://www.bridgeworld.com/pages/readingroom/bridgedictionary.html

5. Yim, Y. et al. *Evaluating and Enhancing LLMs Agent based on Theory of Mind in Guandan*. 说明掼蛋需要结合规则、当前状态、历史行为以及对队友和对手信念的建模；论文也显示纯 LLM 面对巨大动作空间存在明显限制。  
   https://arxiv.org/abs/2408.02559

6. Chen, Z.-S. et al. *基于反绎学习的掼蛋扑克游戏博弈求解*. 使用对局信息与知识库估计其他玩家手牌，说明“规则知识 + 对局观察 + 隐藏手牌估计”是适合掼蛋的研究方向。  
   https://www.lamda.nju.edu.cn/zhangsq/papers/Guandan_ABL%20_CCFAI2025.pdf

7. Zhao, Y. et al. *DanZero+: Dominating the GuanDan Game through Reinforcement Learning*. 说明掼蛋具有巨大状态空间、动作空间和合作竞争并存等特点。  
   https://arxiv.org/abs/2312.02561
