# **掼蛋语音记牌功能深化编程方案**

---

### 一、核心设计原则

1. **模块化 (Modularity)**：将语音识别、指令解析、游戏状态管理和UI反馈分离，便于开发、测试和维护。
2. **单向数据流 (Unidirectional Data Flow)**：语音输入经过解析后，生成一个标准的指令对象，该对象驱动游戏状态的改变，状态的改变再反映到UI上。流程清晰，易于调试。
3. **即时反馈 (Instant Feedback)**：用户的任何操作（语音输入、识别结果、成功或失败）都应立即在界面上给出明确的反馈。

### 二、数据结构定义 (Data Structures)

这是整个系统的血液，我们需要定义好流动的数据格式。建议使用 TypeScript 来确保类型安全。

TypeScript

`// types/voice-command.ts

// 1. 玩家标识符 export type PlayerIdentifier = 'me' | 'partner' | 'nextPlayer' | 'prevPlayer';

// 2. 动作类型 export type ActionType = 'PLAY' | 'PASS' | 'UNDO';

// 3. 牌型定义 export type CardPattern = | 'single' | 'pair' | 'triple' | 'full_house' | 'straight' | 'consecutive_triples' // 飞机/钢板 | 'bomb' | 'flush_straight' | 'joker_bomb';

// 4. 解析后的指令对象（最核心的数据结构） export interface ParsedCommand { player: PlayerIdentifier; // 出牌玩家 action: ActionType; // 执行的动作 playDetails?: { // 如果是出牌动作，则包含此详情 pattern: CardPattern; // 牌型 // 示例: 三个八带对五 -> "三个八带对五" rawDescription: string; // 语音识别出的原始描述 // 示例: [H8, S8, D8, H5, C5] cards: string[]; // (可选) 解析出的具体卡牌ID，如果需要校验逻辑 }; }`

### 三、规范化指令集 (The Command Book)

**【新增部分】** 这是整个语音识别功能的基础，定义了用户需要遵守的报牌“语言”。

**统一规范：**

- **玩家前缀**：每条出牌指令都必须以 `我：`、`对家：`、`下家：`、`上家：` 开头。
- **牌面表示**：为了解析方便，牌面统一使用数字和特定字符。建议App界面提示用户按此标准播报。
    - **数字牌**: 直接说数字，如 `2, 3, 4, 5, 6, 7, 8, 9, 10`
    - **花牌**: `J, Q, K, A` (或 `勾, 圈, K, 尖`)
    - **级牌**: 当前级数的数字
    - **王**: `小王, 大王`
- **数量**: 使用中文数字，如 `三, 四, 五`。

|**类别**|**牌型/操作**|**规范指令格式**|**示例**|
|---|---|---|---|
|**基本牌型**|单张|`[玩家]: 单张 [牌面]`|`上家: 单张K`|
||对子|`[玩家]: 对[牌面]`|`我: 对10`|
||三张|`[玩家]: 三个 [牌面]`|`下家: 三个8`|
|**组合牌型**|三带二|`[玩家]: 三个 [牌面] 带对 [牌面]`|`对家: 三个A带对5`|
||顺子|`[玩家]: 顺子 从[牌面]到[牌面]`|`我: 顺子从10到A`|
||飞机(钢板)|`[玩家]: 飞机 从[牌面]到[牌面]`|`下家: 双飞从8到9`|
|**炸弹**|四张炸弹|`[玩家]: 4个 [牌面]`|`对家: 4个8`|
||多张炸弹|`[玩家]: [数量]个 [牌面]`|`上家: 5个J`|
||同花顺|`[玩家]: 同花顺 从[牌面]到[牌面]`|`我: 同花顺从10到A`|
||王炸|`[玩家]: 王炸`|`下家: 王炸`|
|红心配|因红心配识别复杂，不用语音识别，用传统输入方式|||
|**游戏操作**|过牌|`[玩家]: 过`|`对家: 过`|
||撤销|`撤销`|`撤销`|
||重置|`重置`|`重置`|

### 四、模块化编程实现

我们将系统拆分为以下四个核心模块：

### 模块 1：`VoiceRecognitionService` (语音识别服务)

- **职责**：负责启动和停止手机的麦克风，监听用户的声音，调用系统STT引擎，并将识别出的文本结果向外广播。
- **接口设计**：
    - `startListening(onResult: (text: string) => void, onError: (error: string) => void)`: 开始监听。传入成功和失败的回调函数。
    - `stopListening()`: 停止监听。
    - `configureVocabulary(keywords: string[])`: **（关键优化）** 配置识别词库。在开始监听前，将我们指令集的所有关键词（“上家”, “三个”, “带对”, “顺子”等）传入，提高准确率。

### 模块 2：`CommandParser` (指令解析器)

- **职责**：接收 `VoiceRecognitionService` 传来的文本字符串，根据我们预定义的“规范指令集”，将其解析成一个结构化的 `ParsedCommand` 对象。这是我们编码的**核心**。
- **接口设计**：
    - `parse(text: string): ParsedCommand | null`: 解析文本。如果成功，返回 `ParsedCommand` 对象；如果无法识别，返回 `null`。

### 模块 3：`GameStateManager` (游戏状态管理器)

- **职责**：您现有的游戏逻辑核心。它接收 `CommandParser` 生成的 `ParsedCommand` 对象，并执行真正的游戏逻辑。
- **接口设计**：
    - `applyCommand(command: ParsedCommand): { success: boolean, reason?: string }`: 应用指令。
        - **校验逻辑**：在此函数内，需要判断该出牌是否符合掼蛋的游戏规则（例如，上家出了对K，你不能出对J）。
        - **状态更新**：如果校验成功，则更新各家剩余牌数、已出牌记录等。
        - **返回结果**：返回操作是否成功，如果失败，给出原因（如“牌型不符合规则”）。

### 模块 4：`UIFeedbackController` (UI与反馈控制器)

- **职责**：负责所有与用户交互的界面展示和声音反馈。
- **接口设计**：
    - `showListeningState()`: 显示“正在聆听...”的动画。
    - `showRecognizedText(text: string)`: 在界面上临时显示识别出的原文。
    - `showCommandResult(command: ParsedCommand)`: 在牌桌上以动画形式展示出牌结果。
    - `showError(message: string)`: 显示错误信息，如“无法识别指令”或“出牌不符合规则”。
    - `playFeedbackSound(sound: 'success' | 'error')`: 播放成功或失败的提示音。

### 五、完整工作流程

1. 用户点击“语音记牌”按钮。
    
2. `UIFeedbackController`调用`showListeningState()`，同时调用`VoiceRecognitionService.startListening()`。
    
3. 用户报出规范指令，如：“**下家：三个A带对二**”。
    
4. `VoiceRecognitionService`识别出文本，通过`onResult`回调函数将文本`"下家三个A带对二"`传出。
    
5. 该文本被送入 `CommandParser.parse()`。
    
6. `CommandParser`成功解析，返回一个`ParsedCommand`对象： JSON
    
    `{ player: 'nextPlayer', action: 'PLAY', playDetails: { pattern: 'full_house', rawDescription: '三个A带对二', cards: [] // cards可以后续生成或暂时留空 } }`
    
7. 该`ParsedCommand`对象被送入 `GameStateManager.applyCommand()`。
    
8. `GameStateManager`校验此出牌合法，更新游戏状态，并返回`{ success: true }`。
    
9. 根据成功结果，`UIFeedbackController`调用`showCommandResult()`在界面上展示下家出牌的动画，并调用`playFeedbackSound('success')`。
    
10. 系统等待下一轮语音输入。
    

### 六、关键代码示例 (`CommandParser` 实现)

`CommandParser` 的实现现在可以严格按照 **第三节** 定义的指令集来编写。

TypeScript

`// CommandParser.ts

import { ParsedCommand, PlayerIdentifier, CardPattern, ActionType } from './types/voice-command';

// 定义正则表达式规则和对应解析器的映射，严格对应指令集 const commandRules = [ // 规则顺序很重要，优先匹配更复杂、更具体的规则 { name: 'full_house', pattern: /三个(.+?)带对(.+)/, parser: (player, match) => ({ player, action: 'PLAY' as ActionType, playDetails: { pattern: 'full_house' as CardPattern, rawDescription: match[0] } }) }, { name: 'straight', pattern: /(\d+)张顺子从(.+?)到(.+)/, parser: (player, match) => ({ player, action: 'PLAY' as ActionType, playDetails: { pattern: 'straight' as CardPattern, rawDescription: match[0] } }) }, { name: 'bomb', pattern: /(\d+)个(.+)/, parser: (player, match) => ({ player, action: 'PLAY' as ActionType, playDetails: { pattern: 'bomb' as CardPattern, rawDescription: match[0] } }) }, // ... 其他牌型的规则，如对子、单张等 { name: 'pass', pattern: /^过$/, parser: (player, match) => ({ player, action: 'PASS' as ActionType }) } ];

export class CommandParser { // 主解析函数 public parse(text: string): ParsedCommand | null { text = text.trim();

```
// 1. 解析玩家
let player: PlayerIdentifier | null = null;
let actionText: string = '';

if (text.startsWith('我：')) {
  player = 'me';
  actionText = text.substring(2);
} else if (text.startsWith('对家：')) {
  player = 'partner';
  actionText = text.substring(3);
} else if (text.startsWith('下家：')) {
  player = 'nextPlayer';
  actionText = text.substring(3);
} else if (text.startsWith('上家：')) {
  player = 'prevPlayer';
  actionText = text.substring(3);
} else if (text === '撤销') {
  // 处理全局指令
  return { player: 'me', action: 'UNDO' as ActionType }; // 假设撤销总是当前用户的操作
}

if (!player || !actionText) return null; // 如果没有识别出玩家或动作，则认为是无效指令

// 2. 匹配并解析动作
for (const rule of commandRules) {
  const match = actionText.match(rule.pattern);
  if (match) {
    console.log(`Matched rule: ${rule.name}`);
    return rule.parser(player, match);
  }
}

return null; // 所有规则都未匹配
```

} }`

### 七、错误处理与用户体验

- **无法解析**：`CommandParser.parse()`返回`null`时，`UIFeedbackController`应提示：“无法理解，请参照指令手册报牌”。
- **出牌违规**：`GameStateManager.applyCommand()`返回`{ success: false }`时，`UIFeedbackController`应提示失败原因，如：“出牌无效，对K需要更大的对子来压”。
- **语音识别失败**：`VoiceRecognitionService`的`onError`被触发时，`UIFeedbackController`应提示：“语音识别失败，请检查网络或麦克风权限”。

### 八、总结与下一步

这份补充了“指令集”的方案现在更加完整和可执行。您的首要任务是：

1. **最终确定** `第三节` 中的所有指令，确保覆盖所有情况且无歧义。
2. **重点开发** `第六节` 中的 `CommandParser`，并使用固定的测试字符串来验证其解析的准确性。
3. 在解析器稳定后，再**集成**语音识别服务和游戏状态管理器。